import React from 'react';

const items = props.news.map((item) => {
     return(
         <div>
             <h3>
                 {item.title}
             </h3>
             <div>
                 {item.feed}
             </div>
         </div>
     )   
    });

    for(i=0; i<array; i++){
        array[i].item.title
    } 
return(items)
}

export default NewsList;
